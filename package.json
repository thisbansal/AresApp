{
  "name": "sampleapp",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "move": "rm -rf ./webos-meta/dist && mv ./dist/ ./webos-meta/",
    "preview": "vite preview",
    "start-remote": "ares-package ./webos-meta/ && ares-install --device lg ./*.ipk && ares-launch --device lg com.nookbyte.aresapp.v1 && BROWSER=chrome ares-inspect --device lg --app com.nookbyte.aresapp.v1 --open",
    "start": "ares-package ./webos-meta/ && ares-install --device lg ./*.ipk && ares-launch --device lg com.nookbyte.aresapp.v1",
    "install": "ares-package ./webos-meta/ && ares-install --device lg ./*.ipk"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.1.2",
    "vite": "^7.2.4"
  },
  "dependencies": {
    "react": "^19.2.3",
    "react-dom": "^19.2.3",
    "react-router-dom": "^7.12.0",
    "zustand": "^5.0.10"
  }
}
